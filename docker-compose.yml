services:

  gateway:
    build: ./services/gateway
    ports:
      - "8000:8000"
    platform: linux/amd64
    depends_on:
      - redis

  redis:
    image: redis:7
    ports:
      - "6379:6379"
  
  orchestrator:
    build: ./services/orchestrator
    depends_on:
      - redis